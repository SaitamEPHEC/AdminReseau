version: '3'

services:
 user1:
    image: saitam/user:latest
    container_name: userTest
    dns: 
    - 172.16.0.3
    - 8.8.8.8
    dns_search: intranet
    networks:
      custom_network:
        ipv4_address: 172.16.2.1
    tty: true
        
networks:
  custom_network:
    external:
      name: administration
